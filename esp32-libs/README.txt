You can use these precompiled optimized (bigger receive buffer) libraries for Arduino, or compile them yourself with this Docker image: https://hub.docker.com/r/espressif/esp32-arduino-lib-builder

You need to replace the Arduino esp32 library files with these.
